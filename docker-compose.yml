
services:
  app:
    build: .
    container_name: whatsapp-logger-fixed
    restart: unless-stopped
    env_file:
      - .env
    ports:
      - "${PORT:-10000}:10000"
    environment:
      - TZ=America/Sao_Paulo
    volumes:
      - ./data:/app/data
    shm_size: "1gb"
